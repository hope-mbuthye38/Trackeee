import { TrackeeysDirective } from './trackeeys.directive';

describe('TrackeeysDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackeeysDirective();
    expect(directive).toBeTruthy();
  });
});
