import { TrackleeDirective } from './tracklee.directive';

describe('TrackleeDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackleeDirective();
    expect(directive).toBeTruthy();
  });
});
